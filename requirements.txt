# Sentinel — dependencias
# Instalar con:  pip install -r requirements.txt   (o: uv pip install -r requirements.txt)

torch>=2.0            # red neuronal (MLP). Instala la build de CPU/GPU según la máquina.
pandas>=2.0           # manejo de datos tabulares
numpy>=1.24           # operaciones numéricas
scikit-learn>=1.3     # split, escalado y métricas (NO el modelo, que es PyTorch)
matplotlib>=3.7       # gráficas del EDA y matriz de confusión
seaborn>=0.12         # visualización
joblib>=1.3           # guardar/cargar el scaler
streamlit>=1.30       # interfaz web
openai>=1.0           # LLM del asistente RAG (opcional; degrada sin OPENAI_API_KEY)
